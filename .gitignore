result
result-*

# Cargo build artifacts. Cargo.lock is intentionally NOT ignored -- it's
# committed so `nix build` (rustPlatform.buildRustPackage's cargoLock)
# works fully offline and reproducibly.
/target

# Cloudflare wrangler local cache (holds account id + name — never commit)
.wrangler/
